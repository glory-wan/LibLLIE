# Log

Target: `log`

```bash
libllie predict log input.jpg -o results/log/output.jpg --kwargs gain=10.0
```

Parameters passed with `--kwargs`:

| Parameter | Type | Default | Meaning |
| --- | --- | --- | --- |
| `gain` | `float` | `10.0` | Logarithmic enhancement gain, must be positive |
